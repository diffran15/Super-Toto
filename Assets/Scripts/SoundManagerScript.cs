using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoundManagerScript : MonoBehaviour
{
	/*public static AudioClip playerhitSound, fireSound, jumpSound, enemydeathSound, CoinPickUpSound;
	static AudioSource audioSrc;
    // Start is called before the first frame update
    void Start()
    {
		playerhitSound = Resources.Load<AudioClip>("PlayerHit");
		fireSound = Resources.Load<AudioCLip>("fire");
		jumpSound = Resources.Load<AudioClip>("jump");
		enemydeathSound = Resources.Load<AudioClip>("enemyDeath");
		CoinPickUpSound = Resources.Load<AudioClip>("CoinPickUp");
    }

    // Update is called once per frame
    void Update()
    {
        
    }

	public static void PlaySound (string clip)
	{
		switch (clip)
		{
			case "fire":
				audioSrc.PlayOnShot(fireSound);
				break;
			case "jump":
				audioSrc.PlayOnSHot(jumpSound);
				break;
			case "PlayerHit":
				audioSrc.PlayOnShot(playerhitSound);
				break;
			case "enemyDeath":
				audioSrc.PlayOnShot(enemydeathSound);
				break;
			case "CoinPickUp":
				audioSrc.PlayOnShot(CoinPickUpSound);
				break;
		}
	} */
}
